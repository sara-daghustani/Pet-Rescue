
<body> 
    <div class="container">  
        <header class="jumbotron bg my-5">
            <h1 class="display-4 text-white">Pet Rescue!</h1>
            <p class="lead text-white"> Losing Your Baby can be scary and painful experience,<br> 
            This is why we are here to reunite owners with their pets.</p>
            <br class="my-5">
            <p class="lead ">
                <%=link_to "Report A Lost or Found Pet!", new_report_path, class:"btn btn-light"%> 
            </p>
        </header> 
        <div class= "cardBody">
        Search Bar
            <div class="row text-center">
                <% @reports.each do |report| %>
                    <div class="col-lg-3 col-md-6 mb-4 ">
                        <div class="card h-100">
                            <div class=reportimg>
                                <%= image_tag(report.img, alt: "Pet Image", class:"card-img-top", height:"200px") %>
                                <div class="top-left"> 
                                    <% if report.reporttype == "Lost"%> 
                                            <h5 class= "none btn btn-danger btn-sm disabled " > 
                                                <%=report.reporttype%>
                                            </h5> 
                                    <%else%>
                                            <h5 class= "none btn btn-success btn-sm disabled" > 
                                                <%=report.reporttype%>
                                            </h5>
                                    <%end%>     
                                </div>
                                <small > Created by <%= report.user.email%>
                                On <%=report.created_at.strftime("%d/%m/%Y")%>
                                </small> 
                            </div>
                            <div class= "text-left font-weight-bold text-gray" style="padding-left: 15px;">
                                Pet name: <%= report.petname %><br>
                                Pet <%=report.reporttype%> Location: <br>
                                <%= report.address %>, <%= report.city %><br>
                                Pet <%=report.reporttype%> Date: <%= report.date %>
                            </div>
                                <%= link_to "Find Out More!", report_path(report), class:"btn btn-light" %>
                            
                        </div>
                    </div>
                <%end%>
            </div>
        </div>
    </div> 
</body>
 
                
                 